export enum Statuses {
	LOADING = 'loading',
	IDLE = 'idle',
	ERROR = 'error',
}
export enum Categories {
	ALL = 'all',
	ART = 'art',
	BIOGRAPHY = 'biography',
	COMPUTERS = 'computers',
	HISTORY = 'history',
	MEDICAL = 'medical',
	POETRY = 'poetry',
}

export enum SortOptions {
	RELEVANCE = 'relevance',
	NEWEST = 'newest',
}
